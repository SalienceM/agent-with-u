{
  "$schema": "https://schema.tauri.app/config/2/capability.json",
  "identifier": "smooth-region",
  "description": "Transparent Smooth screenshot-region selector window",
  "platforms": ["windows"],
  "windows": ["smooth-region"],
  "permissions": [
    "core:default",
    "core:window:allow-destroy",
    "core:window:allow-set-ignore-cursor-events"
  ]
}
