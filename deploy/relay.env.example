# 复制为 /etc/agent-with-u/relay.env，并把 token 换成随机长字符串。
AGENT_WITH_U_RELAY_BIND=127.0.0.1
AGENT_WITH_U_RELAY_PORT=44360
AGENT_WITH_U_RELAY_TOKEN=replace-with-a-random-token
# 用户档案、用户 token 摘要和执行端授权。文件不存在时保持旧版单 token 模式。
AGENT_WITH_U_RELAY_USERS_FILE=/var/lib/agent-with-u-relay/users.json
