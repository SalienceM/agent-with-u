---
name: football-search
description: 搜索天下足球网的足球视频资源（录像、集锦、直播）。当用户要找足球比赛视频、比赛回放、进球集锦、直播源时调用。触发词包括：足球录像、比赛回放、集锦、足球直播、看球、比赛视频，以及提到具体球队名、联赛名并想看视频时也应触发。
type: python-script
input_schema:
  type: object
  properties:
    query:
      type: string
      description: 空格分隔的足球关键词，如"皇马 巴萨"或"欧冠 决赛 2024"
    category:
      type: string
      description: "资源类型：replay=录像 highlight=集锦 live=直播，不填则全部"
  required:
    - query
---

# Football Search

搜索天下足球网的足球视频资源。

## 使用方法

使用 Bash 工具执行以下命令（Python 路径由安装环境自动填充）：

```bash
"<PYTHON>" .claude/skills/football-search/_call.py "<QUERY>" "<CATEGORY>"
```

## QUERY 构造规则（关键）

**用最少的词命中最多的结果。** 搜索引擎压力大、精确词反而漏结果，因此关键词**越少越好**。

### 核心原则

1. **默认只用 1 个词**：联赛名或球队名，用户自行筛选结果
2. **最多 2 个词**：仅当用户明确指定了两方（如两支球队对阵）才加第二个词
3. **不加时间/赛季**：除非用户要找某年的特定决赛
4. **不加轮次/阶段**：用户可以自己看标题筛选

### 示例

| 用户说的 | QUERY | CATEGORY |
|---------|-------|----------|
| 找欧冠视频 | `欧冠` | （不填） |
| 想看皇马对巴萨的录像 | `皇马 巴萨` | `replay` |
| 找梅西的进球集锦 | `梅西` | `highlight` |
| 最近英超曼城的比赛 | `曼城` | （不填） |
| 今晚有什么球赛直播 | `直播` | `live` |
| 2024欧冠决赛集锦 | `欧冠 决赛` | `highlight` |
| 看看哈兰德的进球 | `哈兰德` | （不填） |

## 参数说明

| 参数 | 必填 | 说明 |
|------|------|------|
| QUERY | 是 | **1 个词为主，最多 2 个词**；每词 2-4 字，不加时间不加轮次 |
| CATEGORY | 否 | `replay`=录像　`highlight`=集锦　`live`=直播，不填则搜索全部类型 |

## 规则

- **只执行一次**，将输出直接原样呈现给用户
- 不要重试、不要自行判断结果质量、不要额外处理输出
- 关键词越少越好：默认 1 个词，最多 2 个词，不加年份和轮次
